{
  "name": "autodl-api",
  "version": "1.0.0",
  "description": "AutoDL Video Downloader API",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "apt update && apt install -y ffmpeg python3-pip && pip install yt-dlp && node server.js"
  },
  "dependencies": {
    "@distube/ytdl-core": "^4.16.12",
    "cors": "^2.8.6",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "node-cache": "^5.1.2",
    "yt-search": "^2.13.1"
  }
}
